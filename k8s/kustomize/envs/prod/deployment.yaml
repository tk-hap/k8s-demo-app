apiVersion: apps/v1
kind: Deployment
metadata:
  name: petclinic-deployment
spec:
  template:
    spec:
      containers:
      - name: petclinic
        resources:
          requests:
            cpu: "200m"
            memory: "750Mi"
          limits:
            cpu: "300m"
            memory: "750Mi"